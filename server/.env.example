DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE_NAME"
PORT=5000
CLIENT_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Required only to create the first ADMIN account via POST /api/users.
# Remove or rotate it after the first ADMIN exists.
USER_BOOTSTRAP_KEY="beernoppoltp96090"
